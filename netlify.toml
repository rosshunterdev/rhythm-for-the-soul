[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Redirect Netlify's default 404 to our custom page (Astro generates 404.html)
[[redirects]]
  from = "/*"
  to = "/404"
  status = 404
